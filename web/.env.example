# Laravel API base. The /api/v1 suffix is optional — it is added when missing.
# This is the variable to set in Vercel (all environments).
NEXT_PUBLIC_API_URL=https://api.m-kopatz.co.tz/api/v1

# Local Laravel instead:
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
